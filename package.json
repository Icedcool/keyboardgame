{
  "name": "animal-keys",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A keyboard learning game for kids — press A-Z, meet animals.",
  "scripts": {
    "predev": "node scripts/build-catalog.js",
    "dev": "vite",
    "prebuild": "node scripts/build-catalog.js",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "catalog": "node scripts/build-catalog.js",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.20.5"
  }
}
